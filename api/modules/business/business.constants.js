import { BUSINESS_TYPES, MODULES } from "../../shared/constants/index.js";

export const BUSINESS_MESSAGES = Object.freeze({
  CREATED: "Business created successfully.",
  UPDATED: "Business updated successfully.",
  FETCHED: "Business fetched successfully.",
  MODULES_UPDATED: "Business modules updated successfully.",

  NOT_FOUND: "Business not found.",
  SLUG_EXISTS: "Business slug already exists.",
  OWNER_ALREADY_HAS_BUSINESS: "Business owner already owns a business.",
});

export const DEFAULT_MODULES = Object.freeze({
  [BUSINESS_TYPES.GROCERY]: [
    MODULES.PRODUCTS,
    MODULES.INVENTORY,
    MODULES.SALES,
    MODULES.CUSTOMERS,
    MODULES.EMPLOYEES,
    MODULES.REPORTS,
    MODULES.DASHBOARD,
    MODULES.SETTINGS,
  ],

  [BUSINESS_TYPES.PHARMACY]: [
    MODULES.PRODUCTS,
    MODULES.INVENTORY,
    MODULES.SALES,
    MODULES.CUSTOMERS,
    MODULES.EMPLOYEES,
    MODULES.REPORTS,
    MODULES.DASHBOARD,
    MODULES.SETTINGS,
  ],

  [BUSINESS_TYPES.RESTAURANT]: [
    MODULES.PRODUCTS,
    MODULES.INVENTORY,
    MODULES.SALES,
    MODULES.EMPLOYEES,
    MODULES.REPORTS,
    MODULES.DASHBOARD,
    MODULES.SETTINGS,
  ],

  [BUSINESS_TYPES.SALON]: [
    MODULES.SERVICES,
    MODULES.APPOINTMENTS,
    MODULES.STAFF_SCHEDULING,
    MODULES.COMMISSIONS,
    MODULES.MEMBERSHIPS,
    MODULES.CUSTOMERS,
    MODULES.EMPLOYEES,
    MODULES.REPORTS,
    MODULES.DASHBOARD,
    MODULES.SETTINGS,
  ],

  [BUSINESS_TYPES.SMALL_SHOP]: [
    MODULES.PRODUCTS,
    MODULES.INVENTORY,
    MODULES.SALES,
    MODULES.REPORTS,
    MODULES.DASHBOARD,
    MODULES.SETTINGS,
  ],
});
